#Single Element Vector
x = c(1.1)
print(x)

#Multiple Elements Vector
y <- c(1, 3, 2, 5)
print(y)

z <- c(4, 2, 3, 0)

#Check x, y and 7 length using length() function
length(x)
length(y)
length(z)
#Addition result of y + z
y + z
#Look at a list of all of the objects that we have saved so far
ls()

#Delete object that we don't want
rm(x)

#Again, look at a list of all of the objects that we have saved so far
ls()
